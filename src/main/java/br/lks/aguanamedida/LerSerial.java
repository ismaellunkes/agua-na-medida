package br.lks.aguanamedida;

public class LerSerial {
/*SerialPort comPort = SerialPort.getCommPort("COM3"); // Substitua por sua porta serial
comPort.openPort();
comPort.setComPortTimeouts(SerialPort.TIMEOUT_READ_SEMI_BLOCKING,0,0);

while(comPort.isOpen())

    {
        if (comPort.bytesAvailable() > 0) {
            byte[] readBuffer = new byte[comPort.bytesAvailable()];
            int numRead = comPort.readBytes(readBuffer,
                    readBuffer.length);
            String dadosSensor = new String(readBuffer, 0, numRead);

            // Processa os dados recebidos (dadosSensor) e envia para a API
            // ...
        }
    }

comPort.closePort();*/

}
